import { Url } from "url";

export class Story {
    id: string;
    name: string;
    author: string;
    title: string;
    description: string;
    url: string;
    urlToImage: string;
    //publishedAt: DateTime;
  }